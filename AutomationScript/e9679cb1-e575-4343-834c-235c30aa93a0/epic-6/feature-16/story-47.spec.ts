import { test, expect } from '@playwright/test';

test('As Attendance System Integrator, I want to connect biometric devices to the system to achieve real-time attendance data capture', async ({ page }) => {
  // TODO: Implement test steps for: As Attendance System Integrator, I want to connect biometric devices to the system to achieve real-time attendance data capture
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});