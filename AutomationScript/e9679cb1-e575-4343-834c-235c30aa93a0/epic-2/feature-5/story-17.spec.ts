import { test, expect } from '@playwright/test';

test('As Scheduler, I want to receive alerts via multiple channels to ensure I do not miss scheduling conflicts', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to receive alerts via multiple channels to ensure I do not miss scheduling conflicts
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});