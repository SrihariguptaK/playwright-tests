import { test, expect } from '@playwright/test';

test('As Scheduler, I want to receive real-time alerts for scheduling conflicts to avoid double-booking resources', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to receive real-time alerts for scheduling conflicts to avoid double-booking resources
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});