import { test, expect } from '@playwright/test';

test('As Scheduler, I want to detect overlapping appointments in real-time to prevent double bookings', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to detect overlapping appointments in real-time to prevent double bookings
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});