```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-12",
      "storyTitle": "As Scheduler, I want to detect resource double bookings in real-time to avoid allocation conflicts",
      "testFile": "resource-double-booking-detection.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('Resource Double Booking Detection', () => {\n  test.beforeEach(async ({ page }) => {\n    // Navigate to scheduler dashboard and login\n    await page.goto('/scheduler/dashboard');\n    await page.fill('[data-testid=\"username-input\"]', 'scheduler@example.com');\n    await page.fill('[data-testid=\"password-input\"]', 'password123');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page.locator('[data-testid=\"scheduler-dashboard\"]')).toBeVisible();\n  });\n\n  test('should detect room double booking when creating overlapping appointment', async ({ page }) => {\n    // Navigate to create appointment page\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await expect(page.locator('[data-testid=\"appointment-form\"]')).toBeVisible();\n\n    // Fill appointment details with room that is already booked\n    await page.fill('[data-testid=\"appointment-title-input\"]', 'Team Meeting');\n    await page.fill('[data-testid=\"appointment-date-input\"]', '2024-02-15');\n    await page.fill('[data-testid=\"appointment-start-time-input\"]', '10:00');\n    await page.fill('[data-testid=\"appointment-end-time-input\"]', '11:00');\n    \n    // Select room resource that has existing booking\n    await page.click('[data-testid=\"resource-type-dropdown\"]');\n    await page.click('[data-testid=\"resource-type-room\"]');\n    await page.click('[data-testid=\"resource-dropdown\"]');\n    await page.click('[data-testid=\"resource-conference-room-a\"]');\n\n    // Wait for real-time validation\n    await page.waitForTimeout(500);\n\n    // Verify conflict detection\n    await expect(page.locator('[data-testid=\"resource-conflict-alert\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"resource-conflict-alert\"]')).toContainText('Resource conflict detected');\n    await expect(page.locator('[data-testid=\"conflict-details\"]')).toContainText('Conference Room A is already booked');\n    await expect(page.locator('[data-testid=\"conflict-time-range\"]')).toContainText('10:00 - 11:00');\n\n    // Verify save button is disabled\n    await expect(page.locator('[data-testid=\"save-appointment-button\"]')).toBeDisabled();\n  });\n\n  test('should detect equipment double booking when updating existing appointment', async ({ page }) => {\n    // Navigate to appointments list\n    await page.click('[data-testid=\"appointments-list-link\"]');\n    await expect(page.locator('[data-testid=\"appointments-table\"]')).toBeVisible();\n\n    // Select and edit existing appointment\n    await page.click('[data-testid=\"appointment-row-1\"]');\n    await page.click('[data-testid=\"edit-appointment-button\"]');\n    await expect(page.locator('[data-testid=\"appointment-form\"]')).toBeVisible();\n\n    // Change equipment to one that is already booked at this time\n    await page.click('[data-testid=\"resource-type-dropdown\"]');\n    await page.click('[data-testid=\"resource-type-equipment\"]');\n    await page.click('[data-testid=\"resource-dropdown\"]');\n    await page.click('[data-testid=\"resource-projector-1\"]');\n\n    // Wait for real-time validation\n    await page.waitForTimeout(500);\n\n    // Verify conflict detection for equipment\n    await expect(page.locator('[data-testid=\"resource-conflict-alert\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"conflict-details\"]')).toContainText('Projector 1 is already allocated');\n    await expect(page.locator('[data-testid=\"conflict-appointment-id\"]')).toBeVisible();\n\n    // Verify update button is disabled\n    await expect(page.locator('[data-testid=\"update-appointment-button\"]')).toBeDisabled();\n  });\n\n  test('should detect personnel double booking across multiple appointments', async ({ page }) => {\n    // Navigate to create appointment page\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await expect(page.locator('[data-testid=\"appointment-form\"]')).toBeVisible();\n\n    // Fill appointment details\n    await page.fill('[data-testid=\"appointment-title-input\"]', 'Client Consultation');\n    await page.fill('[data-testid=\"appointment-date-input\"]', '2024-02-16');\n    await page.fill('[data-testid=\"appointment-start-time-input\"]', '14:00');\n    await page.fill('[data-testid=\"appointment-end-time-input\"]', '15:30');\n    \n    // Select personnel resource that is already assigned\n    await page.click('[data-testid=\"resource-type-dropdown\"]');\n    await page.click('[data-testid=\"resource-type-personnel\"]');\n    await page.click('[data-testid=\"resource-dropdown\"]');\n    await page.click('[data-testid=\"resource-dr-smith\"]');\n\n    // Wait for real-time validation\n    await page.waitForTimeout(500);\n\n    // Verify conflict detection for personnel\n    await expect(page.locator('[data-testid=\"resource-conflict-alert\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"conflict-details\"]')).toContainText('Dr. Smith is already scheduled');\n    await expect(page.locator('[data-testid=\"conflict-icon\"]')).toHaveClass(/error/);\n\n    // Verify save button is disabled\n    await expect(page.locator('[data-testid=\"save-appointment-button\"]')).toBeDisabled();\n  });\n\n  test('should allow appointment creation when no resource conflicts exist', async ({ page }) => {\n    // Navigate to create appointment page\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await expect(page.locator('[data-testid=\"appointment-form\"]')).toBeVisible();\n\n    // Fill appointment details with available resource\n    await page.fill('[data-testid=\"appointment-title-input\"]', 'Strategy Session');\n    await page.fill('[data-testid=\"appointment-date-input\"]', '2024-02-20');\n    await page.fill('[data-testid=\"appointment-start-time-input\"]', '09:00');\n    await page.fill('[data-testid=\"appointment-end-time-input\"]', '10:00');\n    \n    // Select available room resource\n    await page.click('[data-testid=\"resource-type-dropdown\"]');\n    await page.click('[data-testid=\"resource-type-room\"]');\n    await page.click('[data-testid=\"resource-dropdown\"]');\n    await page.click('[data-testid=\"resource-conference-room-b\"]');\n\n    // Wait for real-time validation\n    await page.waitForTimeout(500);\n\n    // Verify no conflict alert is shown\n    await expect(page.locator('[data-testid=\"resource-conflict-