```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-11",
      "storyTitle": "As Scheduler, I want to detect overlapping appointments in real-time to prevent double bookings",
      "testFile": "overlap-detection.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('Story-11: Real-time Overlapping Appointment Detection', () => {\n  test.beforeEach(async ({ page }) => {\n    // Navigate to scheduler dashboard\n    await page.goto('/scheduler/dashboard');\n    // Login as scheduler if needed\n    await page.fill('[data-testid=\"username-input\"]', 'scheduler@test.com');\n    await page.fill('[data-testid=\"password-input\"]', 'password123');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page.locator('[data-testid=\"scheduler-dashboard\"]')).toBeVisible();\n  });\n\n  test('should detect overlapping appointment for same resource in real-time', async ({ page }) => {\n    // Create first appointment\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await page.fill('[data-testid=\"appointment-title\"]', 'Initial Appointment');\n    await page.fill('[data-testid=\"appointment-resource\"]', 'Conference Room A');\n    await page.fill('[data-testid=\"appointment-date\"]', '2024-02-15');\n    await page.fill('[data-testid=\"appointment-start-time\"]', '10:00');\n    await page.fill('[data-testid=\"appointment-end-time\"]', '11:00');\n    await page.click('[data-testid=\"save-appointment-button\"]');\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeVisible();\n\n    // Attempt to create overlapping appointment\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await page.fill('[data-testid=\"appointment-title\"]', 'Overlapping Appointment');\n    await page.fill('[data-testid=\"appointment-resource\"]', 'Conference Room A');\n    await page.fill('[data-testid=\"appointment-date\"]', '2024-02-15');\n    await page.fill('[data-testid=\"appointment-start-time\"]', '10:30');\n    await page.fill('[data-testid=\"appointment-end-time\"]', '11:30');\n\n    // Verify conflict detection within 1 second\n    const startTime = Date.now();\n    await page.click('[data-testid=\"save-appointment-button\"]');\n    await expect(page.locator('[data-testid=\"conflict-warning\"]')).toBeVisible({ timeout: 1000 });\n    const detectionTime = Date.now() - startTime;\n    expect(detectionTime).toBeLessThan(1000);\n\n    // Verify conflict indicator is visible\n    await expect(page.locator('[data-testid=\"conflict-indicator\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"conflict-message\"]')).toContainText('overlapping');\n  });\n\n  test('should flag conflicting appointments with clear conflict indicators', async ({ page }) => {\n    // Create base appointment\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await page.fill('[data-testid=\"appointment-title\"]', 'Base Meeting');\n    await page.fill('[data-testid=\"appointment-resource\"]', 'Meeting Room B');\n    await page.fill('[data-testid=\"appointment-date\"]', '2024-02-20');\n    await page.fill('[data-testid=\"appointment-start-time\"]', '14:00');\n    await page.fill('[data-testid=\"appointment-end-time\"]', '15:00');\n    await page.click('[data-testid=\"save-appointment-button\"]');\n\n    // Create conflicting appointment\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await page.fill('[data-testid=\"appointment-title\"]', 'Conflicting Meeting');\n    await page.fill('[data-testid=\"appointment-resource\"]', 'Meeting Room B');\n    await page.fill('[data-testid=\"appointment-date\"]', '2024-02-20');\n    await page.fill('[data-testid=\"appointment-start-time\"]', '14:30');\n    await page.fill('[data-testid=\"appointment-end-time\"]', '15:30');\n    await page.click('[data-testid=\"save-appointment-button\"]');\n\n    // Verify conflict flag is displayed\n    await expect(page.locator('[data-testid=\"conflict-flag\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"conflict-flag\"]')).toHaveClass(/conflict-active/);\n\n    // Verify conflict details are shown\n    await expect(page.locator('[data-testid=\"conflict-details\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"conflict-resource\"]')).toContainText('Meeting Room B');\n    await expect(page.locator('[data-testid=\"conflict-time-range\"]')).toContainText('14:30 - 15:00');\n  });\n\n  test('should support detection for multiple calendar types', async ({ page }) => {\n    // Test personal calendar conflict\n    await page.click('[data-testid=\"calendar-type-selector\"]');\n    await page.click('[data-testid=\"personal-calendar-option\"]');\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await page.fill('[data-testid=\"appointment-title\"]', 'Personal Event 1');\n    await page.fill('[data-testid=\"appointment-date\"]', '2024-03-01');\n    await page.fill('[data-testid=\"appointment-start-time\"]', '09:00');\n    await page.fill('[data-testid=\"appointment-end-time\"]', '10:00');\n    await page.click('[data-testid=\"save-appointment-button\"]');\n\n    // Create overlapping personal appointment\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await page.fill('[data-testid=\"appointment-title\"]', 'Personal Event 2');\n    await page.fill('[data-testid=\"appointment-date\"]', '2024-03-01');\n    await page.fill('[data-testid=\"appointment-start-time\"]', '09:30');\n    await page.fill('[data-testid=\"appointment-end-time\"]', '10:30');\n    await page.click('[data-testid=\"save-appointment-button\"]');\n    await expect(page.locator('[data-testid=\"conflict-warning\"]')).toBeVisible();\n\n    // Test resource calendar conflict\n    await page.click('[data-testid=\"calendar-type-selector\"]');\n    await page.click('[data-testid=\"resource-calendar-option\"]');\n    await page.click('[data-testid=\"create-appointment-button\"]');\n    await page.fill('[data-testid=\"appointment-title\"]', 'Resource Booking 1');\n    await page.fill('[data-testid=\"appointment-resource\"]', 'Projector A');\n    await page.fill('[data-testid=\"appointment-date\"]', '2024-03-05');\n    await page.fill('[data-testid=\"appointment-start-time\"]', '13:00');\n    await page.fill('[data-testid=\"appointment-end-time\"]', '14:00');\n    await page.click('[data-testid=\"save-appointment-button\