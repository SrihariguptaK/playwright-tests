import { test, expect } from '@playwright/test';

test('As Scheduler, I want the system to detect overlapping bookings in real-time to prevent scheduling conflicts', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want the system to detect overlapping bookings in real-time to prevent scheduling conflicts
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});