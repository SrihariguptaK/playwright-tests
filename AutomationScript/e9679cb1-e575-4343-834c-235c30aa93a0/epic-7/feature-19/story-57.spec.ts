import { test, expect } from '@playwright/test';

test('As Manager, I want to create tasks with detailed descriptions to achieve clear communication of work requirements', async ({ page }) => {
  // TODO: Implement test steps for: As Manager, I want to create tasks with detailed descriptions to achieve clear communication of work requirements
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});