import { test, expect } from '@playwright/test';

test('As Biometric System Administrator, I want to configure biometric devices to achieve seamless attendance data capture', async ({ page }) => {
  // TODO: Implement test steps for: As Biometric System Administrator, I want to configure biometric devices to achieve seamless attendance data capture
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});