import { test, expect } from '@playwright/test';

test('As HR Manager, I want to generate reports on schedule adherence to monitor compliance and coverage', async ({ page }) => {
  // TODO: Implement test steps for: As HR Manager, I want to generate reports on schedule adherence to monitor compliance and coverage
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});