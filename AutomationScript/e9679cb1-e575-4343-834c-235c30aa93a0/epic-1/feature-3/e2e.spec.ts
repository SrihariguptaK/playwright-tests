```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-6",
      "storyTitle": "As Supervisor, I want to view employee schedules to monitor shift assignments",
      "testFile": "view-employee-schedules.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('View Employee Schedules - Supervisor', () => {\n  test.beforeEach(async ({ page }) => {\n    // Login as Supervisor\n    await page.goto('/login');\n    await page.fill('[data-testid=\"username-input\"]', 'supervisor@company.com');\n    await page.fill('[data-testid=\"password-input\"]', 'SupervisorPass123');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page).toHaveURL(/.*dashboard/);\n  });\n\n  test('should display employee schedules with accurate shift details', async ({ page }) => {\n    // Navigate to schedule view page\n    await page.goto('/schedules');\n    await expect(page.locator('[data-testid=\"schedule-page-title\"]')).toBeVisible();\n    \n    // Verify schedule grid/calendar is displayed\n    await expect(page.locator('[data-testid=\"schedule-view\"]')).toBeVisible();\n    \n    // Verify shift details are displayed\n    const firstShift = page.locator('[data-testid=\"shift-card\"]').first();\n    await expect(firstShift).toBeVisible();\n    \n    // Verify shift contains employee name\n    await expect(firstShift.locator('[data-testid=\"shift-employee-name\"]')).toBeVisible();\n    \n    // Verify shift contains date and time\n    await expect(firstShift.locator('[data-testid=\"shift-date\"]')).toBeVisible();\n    await expect(firstShift.locator('[data-testid=\"shift-time\"]')).toBeVisible();\n    \n    // Verify shift contains department\n    await expect(firstShift.locator('[data-testid=\"shift-department\"]')).toBeVisible();\n    \n    // Verify shift status is displayed\n    await expect(firstShift.locator('[data-testid=\"shift-status\"]')).toBeVisible();\n  });\n\n  test('should filter schedules by employee', async ({ page }) => {\n    await page.goto('/schedules');\n    \n    // Click on employee filter dropdown\n    await page.click('[data-testid=\"employee-filter-dropdown\"]');\n    \n    // Select specific employee\n    await page.click('[data-testid=\"employee-option-john-doe\"]');\n    \n    // Wait for filtered results\n    await page.waitForResponse(response => \n      response.url().includes('/api/schedules') && response.status() === 200\n    );\n    \n    // Verify only selected employee's shifts are displayed\n    const shifts = page.locator('[data-testid=\"shift-card\"]');\n    const shiftCount = await shifts.count();\n    \n    for (let i = 0; i < shiftCount; i++) {\n      const employeeName = await shifts.nth(i).locator('[data-testid=\"shift-employee-name\"]').textContent();\n      expect(employeeName).toContain('John Doe');\n    }\n  });\n\n  test('should filter schedules by date range', async ({ page }) => {\n    await page.goto('/schedules');\n    \n    // Click on date range filter\n    await page.click('[data-testid=\"date-range-filter\"]');\n    \n    // Select start date\n    await page.fill('[data-testid=\"start-date-input\"]', '2024-01-15');\n    \n    // Select end date\n    await page.fill('[data-testid=\"end-date-input\"]', '2024-01-21');\n    \n    // Apply date filter\n    await page.click('[data-testid=\"apply-date-filter-button\"]');\n    \n    // Wait for filtered results\n    await page.waitForResponse(response => \n      response.url().includes('/api/schedules') && response.status() === 200\n    );\n    \n    // Verify shifts are within date range\n    const shifts = page.locator('[data-testid=\"shift-card\"]');\n    await expect(shifts.first()).toBeVisible();\n    \n    // Verify date range indicator is displayed\n    await expect(page.locator('[data-testid=\"active-date-filter\"]')).toContainText('Jan 15 - Jan 21');\n  });\n\n  test('should filter schedules by department', async ({ page }) => {\n    await page.goto('/schedules');\n    \n    // Click on department filter dropdown\n    await page.click('[data-testid=\"department-filter-dropdown\"]');\n    \n    // Select specific department\n    await page.click('[data-testid=\"department-option-sales\"]');\n    \n    // Wait for filtered results\n    await page.waitForResponse(response => \n      response.url().includes('/api/schedules') && response.status() === 200\n    );\n    \n    // Verify only selected department's shifts are displayed\n    const shifts = page.locator('[data-testid=\"shift-card\"]');\n    const shiftCount = await shifts.count();\n    \n    for (let i = 0; i < shiftCount; i++) {\n      const department = await shifts.nth(i).locator('[data-testid=\"shift-department\"]').textContent();\n      expect(department).toContain('Sales');\n    }\n  });\n\n  test('should switch between calendar and list views', async ({ page }) => {\n    await page.goto('/schedules');\n    \n    // Verify default view is displayed\n    await expect(page.locator('[data-testid=\"schedule-view\"]')).toBeVisible();\n    \n    // Switch to calendar view\n    await page.click('[data-testid=\"calendar-view-button\"]');\n    await expect(page.locator('[data-testid=\"calendar-view\"]')).toBeVisible();\n    \n    // Verify calendar grid is displayed\n    await expect(page.locator('[data-testid=\"calendar-grid\"]')).toBeVisible();\n    \n    // Verify calendar shows days of week\n    await expect(page.locator('[data-testid=\"calendar-header\"]')).toBeVisible();\n    \n    // Switch to list view\n    await page.click('[data-testid=\"list-view-button\"]');\n    await expect(page.locator('[data-testid=\"list-view\"]')).toBeVisible();\n    \n    // Verify list format is displayed\n    await expect(page.locator('[data-testid=\"schedule-list\"]')).toBeVisible();\n  });\n\n  test('should display shift exceptions and highlights', async ({ page }) => {\n    await page.goto('/schedules');\n    \n    // Look for shifts with exceptions\n    const exceptionShift = page.locator('[data-testid=\"shift-card\"][data-has-exception=\"true\"]').first();\n    \n    if (await exceptionShift.count() > 0) {\n      // Verify exception indicator is visible\n      await expect(exceptionShift.locator('[data-testid=\"shift-exception-badge\"]')).toBeVisible();\n      \n      // Click on shift to view exception details\n      await exceptionShift.click();\n      \n      // Verify exception details modal/panel appears\n      await expect(page.locator('[data-testid=\"shift-details-modal\"]')).toBeVisible();\n      await expect(page.locator('[data-testid=\"exception-details\"]')).toBeVisible();\n