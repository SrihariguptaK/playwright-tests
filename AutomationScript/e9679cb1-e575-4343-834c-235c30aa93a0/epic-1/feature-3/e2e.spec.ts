```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-10",
      "storyTitle": "As HR Manager, I want to maintain an audit log of schedule changes to ensure compliance",
      "testFile": "audit-log-schedule-changes.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('Audit Log - Schedule Changes Compliance', () => {\n  test.beforeEach(async ({ page }) => {\n    // Login as HR Manager\n    await page.goto('/login');\n    await page.fill('[data-testid=\"email-input\"]', 'hr.manager@company.com');\n    await page.fill('[data-testid=\"password-input\"]', 'Password123!');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page).toHaveURL(/.*dashboard/);\n  });\n\n  test('System logs schedule creation event with user and timestamp', async ({ page }) => {\n    // Navigate to schedule creation page\n    await page.goto('/schedules/create');\n    \n    // Create a new schedule\n    await page.fill('[data-testid=\"employee-select\"]', 'John Doe');\n    await page.fill('[data-testid=\"shift-date\"]', '2024-02-15');\n    await page.fill('[data-testid=\"shift-start-time\"]', '09:00');\n    await page.fill('[data-testid=\"shift-end-time\"]', '17:00');\n    await page.click('[data-testid=\"save-schedule-button\"]');\n    \n    // Wait for success confirmation\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeVisible();\n    \n    // Navigate to audit logs\n    await page.goto('/audit-logs');\n    \n    // Verify the creation event is logged\n    const firstLogEntry = page.locator('[data-testid=\"audit-log-entry\"]').first();\n    await expect(firstLogEntry).toContainText('Schedule Created');\n    await expect(firstLogEntry).toContainText('hr.manager@company.com');\n    await expect(firstLogEntry.locator('[data-testid=\"log-timestamp\"]')).toBeVisible();\n    await expect(firstLogEntry).toContainText('John Doe');\n  });\n\n  test('System logs schedule modification event with user and timestamp', async ({ page }) => {\n    // Navigate to schedules page\n    await page.goto('/schedules');\n    \n    // Select an existing schedule to edit\n    await page.click('[data-testid=\"schedule-row\"]', { position: { x: 10, y: 10 } });\n    await page.click('[data-testid=\"edit-schedule-button\"]');\n    \n    // Modify the schedule\n    await page.fill('[data-testid=\"shift-start-time\"]', '10:00');\n    await page.fill('[data-testid=\"shift-end-time\"]', '18:00');\n    await page.click('[data-testid=\"save-schedule-button\"]');\n    \n    // Wait for success confirmation\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeVisible();\n    \n    // Navigate to audit logs\n    await page.goto('/audit-logs');\n    \n    // Verify the modification event is logged\n    const firstLogEntry = page.locator('[data-testid=\"audit-log-entry\"]').first();\n    await expect(firstLogEntry).toContainText('Schedule Modified');\n    await expect(firstLogEntry).toContainText('hr.manager@company.com');\n    await expect(firstLogEntry.locator('[data-testid=\"log-timestamp\"]')).toBeVisible();\n    await expect(firstLogEntry).toContainText('10:00');\n    await expect(firstLogEntry).toContainText('18:00');\n  });\n\n  test('System logs schedule deletion event with user and timestamp', async ({ page }) => {\n    // Navigate to schedules page\n    await page.goto('/schedules');\n    \n    // Select a schedule to delete\n    const scheduleToDelete = page.locator('[data-testid=\"schedule-row\"]').first();\n    const employeeName = await scheduleToDelete.locator('[data-testid=\"employee-name\"]').textContent();\n    \n    await scheduleToDelete.click();\n    await page.click('[data-testid=\"delete-schedule-button\"]');\n    \n    // Confirm deletion\n    await page.click('[data-testid=\"confirm-delete-button\"]');\n    \n    // Wait for success confirmation\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeVisible();\n    \n    // Navigate to audit logs\n    await page.goto('/audit-logs');\n    \n    // Verify the deletion event is logged\n    const firstLogEntry = page.locator('[data-testid=\"audit-log-entry\"]').first();\n    await expect(firstLogEntry).toContainText('Schedule Deleted');\n    await expect(firstLogEntry).toContainText('hr.manager@company.com');\n    await expect(firstLogEntry.locator('[data-testid=\"log-timestamp\"]')).toBeVisible();\n    await expect(firstLogEntry).toContainText(employeeName || '');\n  });\n\n  test('HR Manager can view and filter audit logs by date range', async ({ page }) => {\n    // Navigate to audit logs page\n    await page.goto('/audit-logs');\n    \n    // Verify audit log page is loaded\n    await expect(page.locator('[data-testid=\"audit-logs-header\"]')).toBeVisible();\n    \n    // Apply date range filter\n    await page.fill('[data-testid=\"filter-start-date\"]', '2024-01-01');\n    await page.fill('[data-testid=\"filter-end-date\"]', '2024-02-28');\n    await page.click('[data-testid=\"apply-filter-button\"]');\n    \n    // Verify filtered results are displayed\n    await expect(page.locator('[data-testid=\"audit-log-entry\"]')).toHaveCount(await page.locator('[data-testid=\"audit-log-entry\"]').count());\n    \n    // Verify all entries are within date range\n    const logEntries = page.locator('[data-testid=\"audit-log-entry\"]');\n    const count = await logEntries.count();\n    \n    for (let i = 0; i < Math.min(count, 5); i++) {\n      const timestamp = await logEntries.nth(i).locator('[data-testid=\"log-timestamp\"]').textContent();\n      expect(timestamp).toBeTruthy();\n    }\n  });\n\n  test('HR Manager can filter audit logs by action type', async ({ page }) => {\n    // Navigate to audit logs page\n    await page.goto('/audit-logs');\n    \n    // Filter by action type - Schedule Created\n    await page.selectOption('[data-testid=\"filter-action-type\"]', 'Schedule Created');\n    await page.click('[data-testid=\"apply-filter-button\"]');\n    \n    // Verify all displayed logs are of type \"Schedule Created\"\n    const logEntries = page.locator('[data-testid=\"audit-log-entry\"]');\n    const count = await logEntries.count();\n    \n    for (let i = 0; i < Math.min(count, 10); i++) {\n      await expect(logEntries.nth(i)).toContainText('Schedule Created');\n    }\n  });\n\n  test('HR Manager can filter audit logs by user', async ({ page }) => {\n    // Navigate to audit logs page\n    await page.goto('/