import { test, expect } from '@playwright/test';

test('As Scheduler, I want to receive notifications about schedule assignments and changes to stay informed', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to receive notifications about schedule assignments and changes to stay informed
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});