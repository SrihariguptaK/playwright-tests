import { test, expect } from '@playwright/test';

test('As Scheduler, I want to modify assigned shifts to accommodate changes and exceptions', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to modify assigned shifts to accommodate changes and exceptions
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});