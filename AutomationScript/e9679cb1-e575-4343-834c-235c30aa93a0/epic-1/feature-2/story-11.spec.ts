import { test, expect } from '@playwright/test';

test('As Scheduler, I want to view schedule conflicts highlighted to quickly identify and resolve issues', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to view schedule conflicts highlighted to quickly identify and resolve issues
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});