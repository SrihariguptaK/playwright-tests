import { test, expect } from '@playwright/test';

test('As Scheduler, I want to view employee schedules in multiple formats to facilitate planning', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to view employee schedules in multiple formats to facilitate planning
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});