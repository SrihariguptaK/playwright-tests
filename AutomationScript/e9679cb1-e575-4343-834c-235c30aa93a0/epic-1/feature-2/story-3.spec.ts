import { test, expect } from '@playwright/test';

test('As Scheduler, I want to assign shift templates to employees to create their work schedules', async ({ page }) => {
  // TODO: Implement test steps for: As Scheduler, I want to assign shift templates to employees to create their work schedules
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});