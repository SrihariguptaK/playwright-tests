```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-2",
      "storyTitle": "As HR Manager, I want to edit existing shift templates to maintain accurate scheduling standards",
      "testFile": "edit-shift-templates.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('Edit Shift Templates - Story 2', () => {\n  test.beforeEach(async ({ page }) => {\n    // Navigate to login page and authenticate as HR Manager\n    await page.goto('/login');\n    await page.fill('[data-testid=\"username-input\"]', 'hr.manager@company.com');\n    await page.fill('[data-testid=\"password-input\"]', 'Password123!');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page).toHaveURL(/.*dashboard/);\n    \n    // Navigate to shift templates page\n    await page.click('[data-testid=\"shift-templates-menu\"]');\n    await expect(page).toHaveURL(/.*shift-templates/);\n  });\n\n  test('should successfully edit an existing shift template with valid data', async ({ page }) => {\n    // Step 1: HR Manager navigates to shift template list\n    await expect(page.locator('[data-testid=\"shift-templates-list\"]')).toBeVisible();\n    \n    // Step 2: Select a template to edit\n    await page.click('[data-testid=\"template-row-1\"] [data-testid=\"edit-button\"]');\n    await expect(page.locator('[data-testid=\"edit-template-form\"]')).toBeVisible();\n    \n    // Verify current template data is loaded\n    await expect(page.locator('[data-testid=\"template-name-input\"]')).toHaveValue('Morning Shift');\n    \n    // Step 3: Modify shift parameters\n    await page.fill('[data-testid=\"template-name-input\"]', 'Updated Morning Shift');\n    await page.fill('[data-testid=\"start-time-input\"]', '08:00');\n    await page.fill('[data-testid=\"end-time-input\"]', '16:30');\n    await page.fill('[data-testid=\"break-duration-input\"]', '45');\n    \n    // Step 4: System validates changes\n    await page.click('[data-testid=\"save-template-button\"]');\n    \n    // Wait for validation to complete\n    await page.waitForResponse(response => \n      response.url().includes('/api/shifttemplates/') && response.status() === 200\n    );\n    \n    // Step 5: Verify template is saved with new version\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"success-message\"]')).toContainText('Shift template updated successfully');\n    \n    // Step 6: Verify confirmation is displayed\n    await expect(page.locator('[data-testid=\"template-version\"]')).toContainText('Version 2');\n    \n    // Verify updated template appears in list with correct data\n    await page.goto('/shift-templates');\n    await expect(page.locator('[data-testid=\"template-row-1\"] [data-testid=\"template-name\"]')).toContainText('Updated Morning Shift');\n    await expect(page.locator('[data-testid=\"template-row-1\"] [data-testid=\"template-time\"]')).toContainText('08:00 - 16:30');\n  });\n\n  test('should prevent editing of template assigned to active schedules', async ({ page }) => {\n    // Navigate to template list\n    await expect(page.locator('[data-testid=\"shift-templates-list\"]')).toBeVisible();\n    \n    // Select a template that is assigned to active schedules\n    await page.click('[data-testid=\"template-row-assigned\"] [data-testid=\"edit-button\"]');\n    \n    // Verify warning message is displayed\n    await expect(page.locator('[data-testid=\"warning-message\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"warning-message\"]')).toContainText('This template is currently assigned to active schedules and cannot be edited');\n    \n    // Verify form fields are disabled\n    await expect(page.locator('[data-testid=\"template-name-input\"]')).toBeDisabled();\n    await expect(page.locator('[data-testid=\"start-time-input\"]')).toBeDisabled();\n    await expect(page.locator('[data-testid=\"end-time-input\"]')).toBeDisabled();\n    \n    // Verify save button is disabled\n    await expect(page.locator('[data-testid=\"save-template-button\"]')).toBeDisabled();\n  });\n\n  test('should validate changes before saving and show validation errors', async ({ page }) => {\n    // Select a template to edit\n    await page.click('[data-testid=\"template-row-1\"] [data-testid=\"edit-button\"]');\n    await expect(page.locator('[data-testid=\"edit-template-form\"]')).toBeVisible();\n    \n    // Enter invalid data - end time before start time\n    await page.fill('[data-testid=\"start-time-input\"]', '16:00');\n    await page.fill('[data-testid=\"end-time-input\"]', '08:00');\n    \n    // Attempt to save\n    await page.click('[data-testid=\"save-template-button\"]');\n    \n    // Verify validation error is displayed\n    await expect(page.locator('[data-testid=\"validation-error\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"validation-error\"]')).toContainText('End time must be after start time');\n    \n    // Verify template is not saved\n    await expect(page.locator('[data-testid=\"success-message\"]')).not.toBeVisible();\n  });\n\n  test('should validate required fields when editing template', async ({ page }) => {\n    // Select a template to edit\n    await page.click('[data-testid=\"template-row-1\"] [data-testid=\"edit-button\"]');\n    await expect(page.locator('[data-testid=\"edit-template-form\"]')).toBeVisible();\n    \n    // Clear required field\n    await page.fill('[data-testid=\"template-name-input\"]', '');\n    \n    // Attempt to save\n    await page.click('[data-testid=\"save-template-button\"]');\n    \n    // Verify validation error for required field\n    await expect(page.locator('[data-testid=\"template-name-error\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"template-name-error\"]')).toContainText('Template name is required');\n    \n    // Verify template is not saved\n    await expect(page.locator('[data-testid=\"success-message\"]')).not.toBeVisible();\n  });\n\n  test('should increment version number upon successful edit', async ({ page }) => {\n    // Select a template to edit\n    await page.click('[data-testid=\"template-row-1\"] [data-testid=\"edit-button\"]');\n    \n    // Verify current version\n    const currentVersion = await page.locator('[data-testid=\"current-version\"]').textContent();\n    expect(currentVersion).toContain('Version 1');\n    \n    // Make valid changes\n    await page.fill('[data-testid=\"template-name-input\"]', 'Updated Template v2');\