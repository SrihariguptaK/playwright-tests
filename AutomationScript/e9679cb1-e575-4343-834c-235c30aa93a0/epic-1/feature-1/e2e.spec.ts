```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-7",
      "storyTitle": "As HR Manager, I want to categorize shift templates to organize scheduling standards",
      "testFile": "shift-template-categories.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('Shift Template Categories Management', () => {\n  test.beforeEach(async ({ page }) => {\n    // Login as HR Manager\n    await page.goto('/login');\n    await page.fill('[data-testid=\"email-input\"]', 'hr.manager@company.com');\n    await page.fill('[data-testid=\"password-input\"]', 'Password123!');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page).toHaveURL(/.*dashboard/);\n    \n    // Navigate to shift template categories management\n    await page.click('[data-testid=\"shift-templates-menu\"]');\n    await page.click('[data-testid=\"categories-submenu\"]');\n    await expect(page).toHaveURL(/.*shift-templates\\/categories/);\n  });\n\n  test('Create new shift template category', async ({ page }) => {\n    // Click create category button\n    await page.click('[data-testid=\"create-category-button\"]');\n    \n    // Fill category details\n    await page.fill('[data-testid=\"category-name-input\"]', 'Nursing Department');\n    await page.fill('[data-testid=\"category-description-input\"]', 'Templates for nursing staff schedules');\n    await page.selectOption('[data-testid=\"category-color-select\"]', 'blue');\n    \n    // Submit category creation\n    await page.click('[data-testid=\"save-category-button\"]');\n    \n    // Verify success message\n    await expect(page.locator('[data-testid=\"success-message\"]')).toContainText('Category created successfully');\n    \n    // Verify category appears in list\n    await expect(page.locator('[data-testid=\"category-list\"]')).toContainText('Nursing Department');\n    await expect(page.locator('[data-testid=\"category-item-nursing-department\"]')).toBeVisible();\n  });\n\n  test('Edit existing shift template category', async ({ page }) => {\n    // Locate existing category\n    const categoryRow = page.locator('[data-testid=\"category-item-emergency\"]');\n    await expect(categoryRow).toBeVisible();\n    \n    // Click edit button\n    await categoryRow.locator('[data-testid=\"edit-category-button\"]').click();\n    \n    // Update category details\n    await page.fill('[data-testid=\"category-name-input\"]', 'Emergency Services');\n    await page.fill('[data-testid=\"category-description-input\"]', 'Updated description for emergency department templates');\n    \n    // Save changes\n    await page.click('[data-testid=\"save-category-button\"]');\n    \n    // Verify success message\n    await expect(page.locator('[data-testid=\"success-message\"]')).toContainText('Category updated successfully');\n    \n    // Verify updated category appears in list\n    await expect(page.locator('[data-testid=\"category-list\"]')).toContainText('Emergency Services');\n  });\n\n  test('Delete shift template category', async ({ page }) => {\n    // Locate category to delete\n    const categoryRow = page.locator('[data-testid=\"category-item-temporary\"]');\n    await expect(categoryRow).toBeVisible();\n    \n    // Click delete button\n    await categoryRow.locator('[data-testid=\"delete-category-button\"]').click();\n    \n    // Confirm deletion in modal\n    await expect(page.locator('[data-testid=\"confirm-delete-modal\"]')).toBeVisible();\n    await page.click('[data-testid=\"confirm-delete-button\"]');\n    \n    // Verify success message\n    await expect(page.locator('[data-testid=\"success-message\"]')).toContainText('Category deleted successfully');\n    \n    // Verify category removed from list\n    await expect(page.locator('[data-testid=\"category-item-temporary\"]')).not.toBeVisible();\n  });\n\n  test('Assign category to shift template', async ({ page }) => {\n    // Navigate to shift templates\n    await page.click('[data-testid=\"templates-tab\"]');\n    await expect(page).toHaveURL(/.*shift-templates/);\n    \n    // Select a shift template\n    const templateRow = page.locator('[data-testid=\"template-item-morning-shift\"]');\n    await templateRow.click();\n    \n    // Open category assignment\n    await page.click('[data-testid=\"assign-category-button\"]');\n    \n    // Select category from dropdown\n    await page.selectOption('[data-testid=\"category-select\"]', 'Nursing Department');\n    \n    // Save assignment\n    await page.click('[data-testid=\"save-assignment-button\"]');\n    \n    // Verify success message\n    await expect(page.locator('[data-testid=\"success-message\"]')).toContainText('Category assigned successfully');\n    \n    // Verify category badge appears on template\n    await expect(templateRow.locator('[data-testid=\"category-badge\"]')).toContainText('Nursing Department');\n  });\n\n  test('Filter shift templates by category', async ({ page }) => {\n    // Navigate to shift templates\n    await page.click('[data-testid=\"templates-tab\"]');\n    \n    // Open category filter\n    await page.click('[data-testid=\"category-filter-button\"]');\n    \n    // Select category to filter\n    await page.click('[data-testid=\"filter-category-nursing-department\"]');\n    \n    // Apply filter\n    await page.click('[data-testid=\"apply-filter-button\"]');\n    \n    // Verify only templates with selected category are displayed\n    const visibleTemplates = page.locator('[data-testid^=\"template-item-\"]');\n    await expect(visibleTemplates).toHaveCount(await visibleTemplates.count());\n    \n    // Verify all visible templates have the correct category badge\n    const categoryBadges = page.locator('[data-testid=\"category-badge\"]');\n    const badgeCount = await categoryBadges.count();\n    for (let i = 0; i < badgeCount; i++) {\n      await expect(categoryBadges.nth(i)).toContainText('Nursing Department');\n    }\n    \n    // Verify filter indicator is active\n    await expect(page.locator('[data-testid=\"active-filter-indicator\"]')).toBeVisible();\n  });\n\n  test('Display categories clearly in UI', async ({ page }) => {\n    // Verify categories section is visible\n    await expect(page.locator('[data-testid=\"categories-section\"]')).toBeVisible();\n    \n    // Verify category list header\n    await expect(page.locator('[data-testid=\"categories-header\"]')).toContainText('Shift Template Categories');\n    \n    // Verify category items display name and description\n    const firstCategory = page.locator('[data-testid=\"category-item\"]').first();\n    await expect(firstCategory.locator('[data-testid=\"category-name\"]')).toBeVisible();\n    await expect(firstCategory.locator('[data-testid=\"category-description\"]')).toBeVisible();\n    \n    // Verify category color indicator\n    await expect(firstCategory.locator('[data-testid=\"category-color-indicator\"]')).