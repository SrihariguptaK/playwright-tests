```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-1",
      "storyTitle": "As HR Manager, I want to create shift templates to achieve standardized scheduling",
      "testFile": "shift-template-creation.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('Shift Template Creation - Story 1', () => {\n  test.beforeEach(async ({ page }) => {\n    // Navigate to login page and authenticate as HR Manager\n    await page.goto('/login');\n    await page.fill('[data-testid=\"username-input\"]', 'hr.manager@company.com');\n    await page.fill('[data-testid=\"password-input\"]', 'SecurePass123!');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page).toHaveURL(/.*dashboard/);\n    \n    // Navigate to shift template creation page\n    await page.click('[data-testid=\"shift-templates-menu\"]');\n    await page.click('[data-testid=\"create-template-button\"]');\n    await expect(page).toHaveURL(/.*shift-templates\\/create/);\n  });\n\n  test('should successfully create a shift template with valid data', async ({ page }) => {\n    // Fill in template name\n    await page.fill('[data-testid=\"template-name-input\"]', 'Morning Shift Template');\n    \n    // Fill in start time\n    await page.fill('[data-testid=\"start-time-input\"]', '08:00');\n    \n    // Fill in end time\n    await page.fill('[data-testid=\"end-time-input\"]', '16:00');\n    \n    // Add break duration\n    await page.fill('[data-testid=\"break-duration-input\"]', '60');\n    \n    // Select role from dropdown\n    await page.click('[data-testid=\"role-select\"]');\n    await page.click('[data-testid=\"role-option-cashier\"]');\n    \n    // Select category\n    await page.click('[data-testid=\"category-select\"]');\n    await page.click('[data-testid=\"category-option-retail\"]');\n    \n    // Add description\n    await page.fill('[data-testid=\"description-input\"]', 'Standard morning shift for retail cashiers');\n    \n    // Submit the form\n    await page.click('[data-testid=\"save-template-button\"]');\n    \n    // Verify success message is displayed\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"success-message\"]')).toContainText('Shift template created successfully');\n    \n    // Verify redirect to templates list\n    await expect(page).toHaveURL(/.*shift-templates/);\n    \n    // Verify the new template appears in the list\n    await expect(page.locator('[data-testid=\"template-list\"]')).toContainText('Morning Shift Template');\n  });\n\n  test('should prevent creation of template with missing required fields', async ({ page }) => {\n    // Leave template name empty\n    await page.fill('[data-testid=\"template-name-input\"]', '');\n    \n    // Fill only start time\n    await page.fill('[data-testid=\"start-time-input\"]', '08:00');\n    \n    // Leave end time empty\n    await page.fill('[data-testid=\"end-time-input\"]', '');\n    \n    // Attempt to submit\n    await page.click('[data-testid=\"save-template-button\"]');\n    \n    // Verify error messages are displayed\n    await expect(page.locator('[data-testid=\"template-name-error\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"template-name-error\"]')).toContainText('Template name is required');\n    \n    await expect(page.locator('[data-testid=\"end-time-error\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"end-time-error\"]')).toContainText('End time is required');\n    \n    // Verify form is not submitted\n    await expect(page).toHaveURL(/.*shift-templates\\/create/);\n    \n    // Verify no success message\n    await expect(page.locator('[data-testid=\"success-message\"]')).not.toBeVisible();\n  });\n\n  test('should prevent creation of template with invalid time format', async ({ page }) => {\n    // Fill in template name\n    await page.fill('[data-testid=\"template-name-input\"]', 'Invalid Time Template');\n    \n    // Enter invalid start time format\n    await page.fill('[data-testid=\"start-time-input\"]', '25:00');\n    \n    // Enter invalid end time format\n    await page.fill('[data-testid=\"end-time-input\"]', '99:99');\n    \n    // Fill break duration\n    await page.fill('[data-testid=\"break-duration-input\"]', '30');\n    \n    // Attempt to submit\n    await page.click('[data-testid=\"save-template-button\"]');\n    \n    // Verify validation error messages\n    await expect(page.locator('[data-testid=\"start-time-error\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"start-time-error\"]')).toContainText('Invalid time format');\n    \n    await expect(page.locator('[data-testid=\"end-time-error\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"end-time-error\"]')).toContainText('Invalid time format');\n    \n    // Verify form remains on creation page\n    await expect(page).toHaveURL(/.*shift-templates\\/create/);\n  });\n\n  test('should prevent creation of template with end time before start time', async ({ page }) => {\n    // Fill in template name\n    await page.fill('[data-testid=\"template-name-input\"]', 'Invalid Time Range Template');\n    \n    // Enter start time\n    await page.fill('[data-testid=\"start-time-input\"]', '16:00');\n    \n    // Enter end time before start time\n    await page.fill('[data-testid=\"end-time-input\"]', '08:00');\n    \n    // Fill break duration\n    await page.fill('[data-testid=\"break-duration-input\"]', '30');\n    \n    // Select role\n    await page.click('[data-testid=\"role-select\"]');\n    await page.click('[data-testid=\"role-option-cashier\"]');\n    \n    // Attempt to submit\n    await page.click('[data-testid=\"save-template-button\"]');\n    \n    // Verify validation error\n    await expect(page.locator('[data-testid=\"time-range-error\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"time-range-error\"]')).toContainText('End time must be after start time');\n    \n    // Verify form is not submitted\n    await expect(page).toHaveURL(/.*shift-templates\\/create/);\n  });\n\n  test('should prevent creation of template with excessive break duration', async ({ page }) => {\n    // Fill in template name\n    await page.fill('[data-testid=\"template-name-input\"]', 'Excessive Break Template');\n    \n    // Enter valid times (8 hour shift)\n    await page.fill('[data-testid=\"start-time-input\"]', '08:00');\n