```json
{
  "success": true,
  "playwrightTests": [
    {
      "storyId": "story-5",
      "storyTitle": "As HR Manager, I want to delete shift templates to remove obsolete scheduling standards",
      "testFile": "delete-shift-templates.spec.ts",
      "testCode": "import { test, expect } from '@playwright/test';\n\ntest.describe('Delete Shift Templates', () => {\n  test.beforeEach(async ({ page }) => {\n    // Login as HR Manager\n    await page.goto('/login');\n    await page.fill('[data-testid=\"username-input\"]', 'hrmanager@company.com');\n    await page.fill('[data-testid=\"password-input\"]', 'password123');\n    await page.click('[data-testid=\"login-button\"]');\n    await expect(page).toHaveURL(/.*dashboard/);\n    \n    // Navigate to shift templates page\n    await page.click('[data-testid=\"shift-templates-menu\"]');\n    await expect(page).toHaveURL(/.*shift-templates/);\n  });\n\n  test('should display shift templates with usage status indicators', async ({ page }) => {\n    // Verify shift templates list is displayed\n    await expect(page.locator('[data-testid=\"shift-templates-list\"]')).toBeVisible();\n    \n    // Verify usage status indicators are present\n    const templateRows = page.locator('[data-testid=\"template-row\"]');\n    await expect(templateRows).toHaveCount(await templateRows.count());\n    \n    // Check first template has usage status\n    const firstTemplate = templateRows.first();\n    await expect(firstTemplate.locator('[data-testid=\"usage-status\"]')).toBeVisible();\n    \n    // Verify status shows either \"In Use\" or \"Not In Use\"\n    const statusText = await firstTemplate.locator('[data-testid=\"usage-status\"]').textContent();\n    expect(statusText).toMatch(/In Use|Not In Use/);\n  });\n\n  test('should prevent deletion of templates currently assigned to schedules', async ({ page }) => {\n    // Find a template marked as \"In Use\"\n    const inUseTemplate = page.locator('[data-testid=\"template-row\"]').filter({ hasText: 'In Use' }).first();\n    await expect(inUseTemplate).toBeVisible();\n    \n    // Click delete button for in-use template\n    await inUseTemplate.locator('[data-testid=\"delete-template-button\"]').click();\n    \n    // Verify error message is displayed\n    await expect(page.locator('[data-testid=\"error-message\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"error-message\"]')).toContainText('Cannot delete template that is currently assigned to schedules');\n    \n    // Verify template still exists in the list\n    await expect(inUseTemplate).toBeVisible();\n  });\n\n  test('should allow deletion of unused templates after confirmation', async ({ page }) => {\n    // Find a template marked as \"Not In Use\"\n    const unusedTemplate = page.locator('[data-testid=\"template-row\"]').filter({ hasText: 'Not In Use' }).first();\n    await expect(unusedTemplate).toBeVisible();\n    \n    // Get template name for verification\n    const templateName = await unusedTemplate.locator('[data-testid=\"template-name\"]').textContent();\n    \n    // Click delete button\n    await unusedTemplate.locator('[data-testid=\"delete-template-button\"]').click();\n    \n    // Verify confirmation dialog appears\n    await expect(page.locator('[data-testid=\"delete-confirmation-dialog\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"confirmation-message\"]')).toContainText('Are you sure you want to delete this shift template?');\n    \n    // Click confirm button\n    await page.click('[data-testid=\"confirm-delete-button\"]');\n    \n    // Wait for deletion to complete\n    await page.waitForResponse(response => response.url().includes('/api/shifttemplates/') && response.status() === 200);\n    \n    // Verify template is removed from list\n    await expect(page.locator('[data-testid=\"template-row\"]').filter({ hasText: templateName || '' })).toHaveCount(0);\n  });\n\n  test('should display confirmation message upon successful deletion', async ({ page }) => {\n    // Find an unused template\n    const unusedTemplate = page.locator('[data-testid=\"template-row\"]').filter({ hasText: 'Not In Use' }).first();\n    await expect(unusedTemplate).toBeVisible();\n    \n    // Click delete button\n    await unusedTemplate.locator('[data-testid=\"delete-template-button\"]').click();\n    \n    // Confirm deletion\n    await page.click('[data-testid=\"confirm-delete-button\"]');\n    \n    // Verify success message is displayed\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeVisible();\n    await expect(page.locator('[data-testid=\"success-message\"]')).toContainText('Shift template deleted successfully');\n    \n    // Verify success message disappears after timeout\n    await expect(page.locator('[data-testid=\"success-message\"]')).toBeHidden({ timeout: 5000 });\n  });\n\n  test('should allow cancellation of deletion from confirmation dialog', async ({ page }) => {\n    // Find an unused template\n    const unusedTemplate = page.locator('[data-testid=\"template-row\"]').filter({ hasText: 'Not In Use' }).first();\n    await expect(unusedTemplate).toBeVisible();\n    \n    // Get template name for verification\n    const templateName = await unusedTemplate.locator('[data-testid=\"template-name\"]').textContent();\n    \n    // Click delete button\n    await unusedTemplate.locator('[data-testid=\"delete-template-button\"]').click();\n    \n    // Verify confirmation dialog appears\n    await expect(page.locator('[data-testid=\"delete-confirmation-dialog\"]')).toBeVisible();\n    \n    // Click cancel button\n    await page.click('[data-testid=\"cancel-delete-button\"]');\n    \n    // Verify dialog is closed\n    await expect(page.locator('[data-testid=\"delete-confirmation-dialog\"]')).toBeHidden();\n    \n    // Verify template still exists in list\n    await expect(page.locator('[data-testid=\"template-row\"]').filter({ hasText: templateName || '' })).toBeVisible();\n  });\n\n  test('should restrict deletion functionality to authorized HR Managers', async ({ page }) => {\n    // Logout as HR Manager\n    await page.click('[data-testid=\"user-menu\"]');\n    await page.click('[data-testid=\"logout-button\"]');\n    \n    // Login as regular employee\n    await page.fill('[data-testid=\"username-input\"]', 'employee@company.com');\n    await page.fill('[data-testid=\"password-input\"]', 'password123');\n    await page.click('[data-testid=\"login-button\"]');\n    \n    // Attempt to navigate to shift templates\n    await page.goto('/shift-templates');\n    \n    // Verify access is denied or delete buttons are not visible\n    const deleteButtons = page.locator('[data-testid=\"delete-template-button\"]');\n    await expect(deleteButtons).toHaveCount(0);\n  });\n\n  test('should complete deletion within 2 seconds performance requirement', async ({ page }) => {\n    // Find an unused template\n    const unusedTemplate = page.loc