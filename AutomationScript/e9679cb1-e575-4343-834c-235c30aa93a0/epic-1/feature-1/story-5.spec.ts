import { test, expect } from '@playwright/test';

test('As HR Manager, I want to delete obsolete shift templates to maintain an accurate template library', async ({ page }) => {
  // TODO: Implement test steps for: As HR Manager, I want to delete obsolete shift templates to maintain an accurate template library
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});