import { test, expect } from '@playwright/test';

test('As HR Manager, I want to validate shift templates to ensure logical consistency and compliance', async ({ page }) => {
  // TODO: Implement test steps for: As HR Manager, I want to validate shift templates to ensure logical consistency and compliance
  // Response parsing failed - please review and implement manually
  await page.goto('/');
  await expect(page).toHaveTitle(/.*/)
});